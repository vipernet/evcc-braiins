name: Build and Push Custom evcc Image

on:
  schedule:
    - cron: '0 3 * * 0' # Automatisch jeden Sonntag um 03:00 Uhr
  workflow_dispatch: # Ermöglicht den manuellen Start per Klick

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1. Holt das offizielle evcc-Repository als Basis
      - name: Checkout official evcc repository
        uses: actions/checkout@v4
        with:
          repository: 'evcc-io/evcc'
          fetch-depth: 1

      # 2. Ermittelt die neueste evcc-Version
      - name: Fetch latest evcc upstream release tag
        id: evcc_tag
        run: |
          TAG=$(curl -s https://github.com/evcc-io/evcc | jq -r .tag_name)
          echo "latest_tag=$TAG" >> $GITHUB_OUTPUT
          echo "Nutze offizielle evcc Version: $TAG"
          # Wechselt den lokalen Code auf die exakte Release-Version
          git checkout tags/$TAG

      # 3. Lädt die Braiins-Dateien vollautomatisch aus dem Quell-Repository herunter
      - name: Download Braiins custom driver files
        run: |
          mkdir -p charger templates/definition/charger
          curl -sSL https://githubusercontent.com -o charger/braiins.go
          curl -sSL https://githubusercontent.com -o templates/definition/charger/braiins.yaml
          echo "Braiins-Erweiterung erfolgreich in den Quellcode injiziert."

      # 4. Login in die GitHub Container Registry
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 5. Bereitet die Build-Umgebung vor
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 6. Baut das Image mit dem injizierten Code und dem originalen Dockerfile von evcc
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.evcc_tag.outputs.latest_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
